import { StyleSheet } from 'react-native';

export const styleSheet = StyleSheet.create({
  scrollView: {
    height: '100%',
    paddingLeft: 16,
    paddingRight: 16,
    paddingBottom: 32,
  },
});
